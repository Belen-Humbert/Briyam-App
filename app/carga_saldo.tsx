import React from 'react';
import { View, Text } from 'react-native';
import styles from '@styles/CargarSaldoScreen.styles';

const CargarSaldoScreen: React.FC = () => {
  return (
    <View style={styles.container}>
      <Text>Pantalla de Cargar Saldo</Text>
    </View>
  );
};

export default CargarSaldoScreen;
