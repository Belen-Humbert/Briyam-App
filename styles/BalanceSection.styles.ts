import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    balanceContainer: {
        padding: 20,
        backgroundColor: '#f5f5f5', // Color de fondo claro
        borderRadius: 10, // Bordes redondeados
        marginBottom: 20, // Espacio inferior entre esta sección y las siguientes
    },
    sectionTitle: {
        fontSize: 20,
        fontWeight: 'bold',
        color: '#333',
        marginBottom: 15, // Espacio debajo del título
        textAlign: 'left',
    },
});

export default styles;
