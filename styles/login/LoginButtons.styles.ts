import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  linkText: {
    color: '#007bff',
    marginBottom: 10,
  },
  customButton: {
    width: '100%',
    marginTop: 5,
  },
});

export default styles;
