import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  logoImage: {
    width: 220,
    height: 220,
    resizeMode: 'contain',
  },
  text: {
    fontSize: 18,
    fontWeight: 'bold',
    color: 'black',
    marginBottom: 30,
    marginTop: -60,
  },
});

export default styles;
