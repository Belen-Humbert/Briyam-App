import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  inputContainer: {
    marginBottom: 15,
    textAlign: 'center'
  },
  label: {
    fontSize: 18, // Aumenta ligeramente el tamaño de la fuente para mayor legibilidad
    marginBottom: 8, // Agrega un poco más de espacio debajo de la etiqueta
    color: '#000000', // Un tono azul que combina bien con los botones y da un toque moderno
    fontWeight: 'bold', // Un peso de fuente medio para darle énfasis
    letterSpacing: 0.5, // Espacio entre letras para mejorar la legibilidad
    textAlign: 'center'
  },
});

export default styles;
