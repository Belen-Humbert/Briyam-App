// UpcomingReservations.styles.ts
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    reservationContainer: {
        flex: 1,
        padding: 16,
        // Agrega más estilos según necesites
    },
    sectionTitle: {
        fontSize: 24,
        fontWeight: 'bold',
        marginBottom: 10,
    },
    noReservationsText: {
        marginTop: 20,
        fontSize: 16,
        color: '#888',
        textAlign: 'center',
    },
    // Agrega más estilos aquí según lo necesites
});

export default styles;
