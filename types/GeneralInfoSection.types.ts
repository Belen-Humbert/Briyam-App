import { MedicoInfo } from "./MedicoInfo.types";

// GeneralInfoSection.types.ts
  export interface GeneralInfoSectionProps {
    isEditMode: boolean;
    medicoInfo?: MedicoInfo;
    marginTop?:string
}
  